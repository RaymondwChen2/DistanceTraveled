import React from 'react'
import MappingRoutes from '../mapping/mapping_index'

const Search = (props) => (
  <div>
    <MappingRoutes/>
    <TheMap props/>
  </div>
);

export default Search